.block
  .secondary-navigation
    %ul.wat-cf
      %li.first.active=link_to pat(:list), url(:users, :index)
      %li=link_to pat(:new), url(:users, :new)
  .content
    %h2.title
      =pat(:all)
      =mt(:users)
    .inner
      %table.table
        %tr
          %th.first=mat(:user, :email)
          %th=mat(:user, :password)
          %th=mat(:user, :name)
          %th=mat(:user, :token)
          %th=mat(:user, :created_at)
          %th=mat(:user, :updated_at)
          %th.last="&nbsp;"
        -@users.each do |user|
          %tr
            %td.first=user.email
            %td=user.password
            %td=user.name
            %td=user.token
            %td=user.created_at
            %td=user.updated_at
            %td.last
              =button_to pat(:edit), url(:users, :edit, :id => user.id), :method => :get, :class => :button_to
              ="|"
              =button_to pat(:delete), url(:users, :destroy, :id => user.id), :method => :delete, :class => :button_to
      .actions-bar.wat-cf
        .actions="&nbsp;"

-content_for :sidebar, partial("base/sidebar")